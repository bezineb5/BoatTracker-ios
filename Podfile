# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BoatTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BoatTracker
  pod 'BarcodeScanner', :git => 'https://github.com/shaqaruden/BarcodeScanner.git'
  pod 'SwiftySuncalc', '~> 1.0'
  pod 'OpenAPIClient', :path => './api-service-swift'

  target 'BoatTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BoatTrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
